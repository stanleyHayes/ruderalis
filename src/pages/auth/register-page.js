import {Box, Container, Typography} from "@mui/material";

const RegisterPage = () => {
    return (
        <Box>
            <Container>
                <Typography sx={{color: 'text.secondary'}} variant="h3">Register Page</Typography>
            </Container>
        </Box>
    )
}

export default RegisterPage;