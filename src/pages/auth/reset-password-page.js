import {Box, Container, Typography} from "@mui/material";

const ResetPasswordPage = () => {
    return (
        <Box>
            <Container>
                <Typography sx={{color: 'text.secondary'}} variant="h3">Reset Password Page</Typography>
            </Container>
        </Box>
    )
}

export default ResetPasswordPage;