import {Box, Container, Typography} from "@mui/material";

const LoginPage = () => {
    return (
        <Box>
            <Container>
                <Typography sx={{color: 'text.secondary'}} variant="h3">Login Page</Typography>
            </Container>
        </Box>
    )
}

export default LoginPage;